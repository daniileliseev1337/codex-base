# Codex Base compatibility bridge

This archived repository exists only so installations pinned to the former
`daniileliseev1337/codex-base` trust identity can verify and install the final
one-time `codex-v0.1.25` organization-migration bridge. Earlier immutable
`codex-v0.1.22` and `codex-v0.1.23` releases are superseded.

The exact source commit declared by the final bridge is retained on the
read-only `bridge-source-codex-v0.1.25` branch for provenance traceability.

Active development and every canonical release are hosted at
<https://github.com/K7-LS/codex-base>.
